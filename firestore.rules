rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Firebase Auth で認証済みのユーザーのみアクセス可能
    // GAS バックエンド（サービスアカウント）はセキュリティルールをバイパスするため影響なし
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
